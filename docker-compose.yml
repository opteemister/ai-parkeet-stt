services:
  parakeet:
    build: .
    container_name: parakeet-stt
    ports:
      - "8000:8000"
    environment:
      - PARAKEET_QUANTIZATION=int8
      - HF_HOME=/models
      - INTRA_THREADS=6
      - INTER_THREADS=6
    volumes:
      - ./models:/models
    restart: unless-stopped
